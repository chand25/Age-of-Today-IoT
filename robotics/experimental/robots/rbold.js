
/*
    birthday: function(){
      let batterylvl = level();

      this.age += 1;

      if(this.alive){
        console.log(`Happy Birthday to You, ${this.name}.`);
        console.log(`You are ${this.age}.`);
      }

      if (batteryLevel > 4){
          this.alive = true;
          this.ready();
       }else{
        this.death();
      }
    }
   });
});

Cylon.start();

*/
